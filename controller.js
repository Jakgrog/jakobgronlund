function Controller(canvas) {

  var program = tdl.programs.loadProgramFromScriptTags("clicked_vs", "clicked_fs")

  /*var arrays = {
  position: new tdl.primitives.AttribBuffer(3, positionData),
  };

  canvas.onmousedown = function(event) {click(event, gl, canvas, positionLoc); };*/
}
